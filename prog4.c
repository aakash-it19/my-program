#include<stdio.h>
void main() {
        int a,b,speed;
        printf("enter the speed1:");
        scanf("%d",&a);
        printf("enter the speed2:");
        scanf("%d",&b);
        speed=(a+b)/2;
        printf("the average speed travelled by the person is %d km/hr",speed);
}
